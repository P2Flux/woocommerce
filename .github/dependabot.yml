# The plugin has no package manifest; only the workflow's own actions need watching. Free.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    open-pull-requests-limit: 2
